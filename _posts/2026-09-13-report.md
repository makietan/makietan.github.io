---
layout: post
title:  "picocolors を使っていたが util.styleText というものが Node.js に追加されたのでそちらに移行するかもしれない"
categories: tech
date: "2026-09-13 00:00:00"
---

<div class="card">
  <a href="https://nodejs.org/api/util.html"></a>
  <div class="card__header">
    <a href="https://nodejs.org/api/util.html">nodejs.org</a>
  </div>
  <div class="card__image">
    <img src="">
  </div>
  <div class="card__title">
    <p>Util | Node.js v26.8.1 Documentation</p>
  </div>
  <div class="card__description">
    <p></p>
  </div>
</div>


外部ライブラリのほうが使いやすいが、Node.js の組み込み関数のほうが依存関係が減るので、そちらに移行するかもしれない。

```javascript
const { styleText } = require('node:util');
const { stderr } = require('node:process');

const successMessage = styleText('green', 'Success!');
console.log(successMessage);

const errorMessage = styleText(
  'red',
  'Error! Error!',
  // Validate if process.stderr has TTY
  { stream: stderr },
);
console.error(errorMessage);
```

ちなみに picocolors は以下のように使っていた。

```javascript
const pico = require('picocolors');

const successMessage = pico.green('Success!');
console.log(successMessage);

const errorMessage = pico.red('Error! Error!');
console.error(errorMessage);
```

こっちのほうがすっくりしとるやないかーい。

